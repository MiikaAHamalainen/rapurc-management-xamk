import { createStyles } from "@material-ui/core";

export const styles = createStyles({

  pdfViewer: {
    height: "100%"
  },

  appBar: {
    position: "relative",
  },

  saveButton: {
    marginLeft: "auto",
    marginRight: "15px"
  }

});
